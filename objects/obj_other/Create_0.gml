scale_multiplier = global.scale_multiplier;
sprite_width_real = sprite_width;
leaving = false;

speed_max = ((y / room_height) * (global.other_speed_default * (2 / 3))) + (global.other_speed_default * (1 / 3));